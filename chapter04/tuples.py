#creating a tuple using () 
t = (1, 2, 3, 4, 5)
# t1 = () #empty tuple
# t1 = (1) # Wrong way to declare a tuple with only one element
t1 = (1,) #tuple with one element
print(t1)

# Printing the elements of a tuple
print(t[0])

#can't update the values of a tuple
# t[0] = 10 #will give an error
